# Backend server URL for Vite dev proxy (development only)
VITE_BACKEND_URL=http://localhost:8000

# Backend API URL for production (when frontend and backend are on different servers)
# Leave empty for development (uses Vite proxy)
# Examples:
#   - Production: VITE_API_URL=https://api.yourdomain.com/api/v1
#   - Staging: VITE_API_URL=https://staging-api.yourdomain.com/api/v1
VITE_API_URL=
